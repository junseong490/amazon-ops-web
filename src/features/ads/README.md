# features/ads — 로드맵 (예약)

광고 최적화(Bulk Operations 왕복) 기능 자리. **MVP 범위 아님** — 폴더/경계만 예약한다(YAGNI, brief LOCKED §16).

재사용 예정 공통 계층: `core/ingest`(업로드·파싱), `core/schema`(헤더 매핑 확장), 순수 계산 함수.
기능 구현은 별도 위임에서 한다.
